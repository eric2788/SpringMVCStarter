<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns="http://www.springframework.org/schema/security" xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:jpa="http://www.springframework.org/schema/data/jpa"
         xmlns:tx="http://www.springframework.org/schema/cache"
         xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd http://www.springframework.org/schema/data/jpa https://www.springframework.org/schema/data/jpa/spring-jpa.xsd http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd">

    <b:bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <b:property name="driverClassName" value="org.apache.derby.jdbc.ClientDriver" />
        <b:property name="url" value="jdbc:derby://localhost:1527/test" />
        <b:property name="username" value="root"/>
        <b:property name="password" value="1234"/>
    </b:bean>

    <!--Create default configuration for Hibernate -->
    <b:bean id="hibernateJpaVendorAdapter" class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>

    <!--Configure the entity manager factory bean -->
    <b:bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <b:property name="dataSource" ref="dataSource"/>
        <b:property name="jpaVendorAdapter" ref="hibernateJpaVendorAdapter"/>
        <!--Set JPA properties -->
        <b:property name="jpaProperties">
            <b:props>
                <b:prop key="hibernate.dialect">org.hibernate.dialect.DerbyDialect</b:prop>
                <b:prop key="hibernate.show_sql">true</b:prop>
                <b:prop key="hibernate.hbm2ddl.auto">update</b:prop>
            </b:props>
        </b:property>
        <!--Set base package of your entities -->
        <b:property name="packagesToScan" value="start.springmvc.model"/>
    </b:bean>

    <!-- Configure the transaction manager bean -->
    <b:bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <b:property name="entityManagerFactory" ref="entityManagerFactory"/>
    </b:bean>

    <http />
    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"/>
        </authentication-provider>
    </authentication-manager>

    <jpa:repositories base-package="start.springmvc.repository"/>

    <tx:annotation-driven />

</b:beans>